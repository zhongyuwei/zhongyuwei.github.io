

## 斑斓之地

考虑欧拉定理，由于抠掉的点是连续的，所以减少的面数就是被抠掉（不再完整）的“四个点的正方形”的数量再加上 1。

## 排座位

每个白点（矩形外的点）只和至多一个黑点相邻

上面和左边均为白点的黑点只有一个

$a_k \ge 0, b_k \ge 1$，所以线段树维护 $a_k + b_k$ 即可。

## 又一道例题

称一个数字是 powerful 的，当且仅当它的每个质数的次数都不小于 $2$。

由于每个 powerful number 都可以写作 $a^2b^3$ 的形式，所以 $n$ 以内的 powerful number 的总数是 $\sum_{a=1}^\sqrt n \sqrt[3]{\frac{n}{a^2}} \approx \sqrt n$ 级别的。

